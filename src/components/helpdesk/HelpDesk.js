import {connect} from "react-redux";
import HelpDeskView from "./HelpDeskView";

const mapStateToProps = null;

const mapDispatchToProps = null;

export default connect(mapStateToProps, mapDispatchToProps)(HelpDeskView);
